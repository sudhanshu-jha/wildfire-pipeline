fastapi==0.115.0
uvicorn[standard]==0.32.0
pydantic==2.9.2
pydantic-settings==2.6.0
nats-py==2.9.0
httpx==0.27.2
# Persistence
asyncpg==0.30.0
# Kafka transport (optional — only needed when TRANSPORT=kafka)
aiokafka==0.11.0
# ONNX inference (optional — only needed when DETECTION_MODEL_PATH is set)
# onnxruntime==1.20.0   # CPU
# onnxruntime-gpu==1.20.0  # GPU
